def longestPalindrome(s: str) -> str:
  array = list(str)
  length = len(array)
  for counter in range(length)
  return False


def is_palindrome(str):
  array = list(str)
  length = len(array)
  for char in range(0, length // 2):
    if array[char] != array[(length - 1) - char]:
      return False
  return True

for i in range(5, 1):
  print(i)

